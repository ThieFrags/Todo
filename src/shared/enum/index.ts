
enum EPriorityTask {
  High = 'High',
  Medium = 'Medium',
  Low = 'Low'
};

enum EProgressTask {
  Completed = 'Completed',
  InProgress = 'In Progress',
  Pending = 'Pending',
};

enum EModalKey {
  Add = 'addTask',
}

export {EProgressTask, EPriorityTask, EModalKey};